# Data Directory

Place official GIS Cup datasets here for local runs. Expected sample name:

```text
data/GIS-cup-sample-dataset.geojson
```

Large geospatial datasets are intentionally ignored by Git. Keep raw official files unchanged; write derived or temporary artifacts under `outputs/` or the OneDrive Parent Folder scratch area.
